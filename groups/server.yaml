server:
  view: yes
  name: "Server"
  icon: mdi:raspberrypi
  entities:
    - group.crypto
    - group.server_actions
    - group.server_internals
    - group.server_network
    - group.server_network_hosts
    - group.weblinks
